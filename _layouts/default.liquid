<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>{{ page.title }}</title>
        <link rel="stylesheet" href="/css/site.css" />
    </head>
    <body>
    <header>
      <h2>{{ page.title }}</h2>
      <img src="/assets/logo.png" width="200">
    </header>
    <main>
      {{ page.content }}
    </main>
  </body>
</html>
